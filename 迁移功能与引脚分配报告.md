# STM32H743 药品配送小车迁移功能与引脚分配报告

## 1. 项目状态

- 目标芯片：STM32H743VITx，封装 LQFP100。
- 当前工程骨架：STM32CubeMX/HAL + MDK-ARM。
- 原 `CODE/` 目录已删除，旧 STM32F407ZG 标准库工程不再保留在项目中。
- 当前编译入口位于 `Core/Src/main.c`，主循环调用 `MedicineCar_Step()`。
- Keil 工程 `MDK-ARM/2021f.uvprojx` 已加入 `medicine_car_app.c` 和 `medicine_car_platform.c`，未引用 F4 标准库、`stm32f4xx` 或 `FWLIB`。

## 2. 主要功能模块

| 模块 | 文件 | 主要职责 | 当前状态 |
|---|---|---|---|
| 主入口 | `Core/Src/main.c` | 初始化 H743 HAL、时钟、GPIO、TIM3、TIM7、USART1，并进入药品配送主循环 | 已接入 |
| 业务流程 | `Core/Src/medicine_car_app.c` | 药品配送路线流程、目标房间判断、送达等待、返航流程 | 已迁移为 H7 可编译版本 |
| 平台适配 | `Core/Src/medicine_car_platform.c` | 电机 PWM、方向 GPIO、八路灰度扫描读取、LED/蜂鸣器、延时、串口打印、编码器桩 | 已接入 |
| 配置开关 | `Core/Inc/medicine_car_config.h` | 缺失硬件开关、默认输入值、PWM/路径/转向参数 | 已接入 |
| 业务接口 | `Core/Inc/medicine_car_app.h` | `MedicineCar_Init()`、`MedicineCar_Step()`、目标房间设置接口 | 已接入 |
| 平台接口 | `Core/Inc/medicine_car_platform.h` | 兼容旧 CODE 的 `Load()`、`Read_Speed()`、`xunxian()`、`turn_left()` 等接口 | 已接入 |

## 3. 功能说明

### 3.1 电机控制

- 使用当前 H743 工程已有的 `TIM3_CH1` 和 `TIM3_CH2` 输出双路 PWM。
- `Load(moto1, moto2)` 接收旧 CODE 风格的正负 PWM 值：
  - 正值：正转。
  - 负值：反转。
  - 0：停止。
- PWM 输入范围限制为 `-8000` 到 `8000`，再映射到 H743 当前 `TIM3` 周期 `1999`。

### 3.2 循迹与灰度传感器

- 当前灰度模块为八路多路选择输出，信号为 `AD0`、`AD1`、`AD2`、`OUT`。
- `AD0/AD1/AD2` 用作通道选择输出，`OUT` 用作当前通道检测结果输入。
- 平台层会依次扫描 0 到 7 共 8 个通道，并缓存到 `gray_sensor_values[0..7]`。
- 旧 CODE 上层仍按 `IO1..IO7` 判断颜色/位置；当前只替换底层读取来源，不改变原先颜色判断逻辑。
- `MED_CAR_GRAY_BLACK_LEVEL` 当前为 `1`，即 `OUT=1` 表示检测到黑线。

### 3.3 药品配送业务

- 支持按目标房间号进入不同路线：
  - `aim == 1`：执行一号房路线。
  - `aim == 2`：执行二号房路线。
  - `aim > 2`：执行扩展路线流程。
- 识别结果通过 `MedicineCar_SetRecognizedNumber()` 或全局变量 `N` 输入。
- 启动路线可通过 `MedicineCar_RequestRun(target_room)` 设置，内部会置位 `Run_Flag`。

### 3.4 未搭建硬件处理

以下模块默认关闭或使用桩实现，配置集中在 `Core/Inc/medicine_car_config.h`：

| 开关 | 默认值 | 说明 |
|---|---:|---|
| `MED_CAR_ENABLE_ENCODER` | 0 | 编码器未接入，当前用虚拟计数保证路线函数不死等 |
| `MED_CAR_ENABLE_BLUETOOTH` | 0 | 蓝牙串口暂未接入 |
| `MED_CAR_ENABLE_RECOGNITION_UART` | 0 | 数字识别串口暂未接入 |
| `MED_CAR_ENABLE_TFT` | 0 | TFT 显示暂未接入 |
| `MED_CAR_ENABLE_KEYS` | 0 | 按键暂未接入 |
| `MED_CAR_ENABLE_DRUG_SENSOR` | 0 | 药品检测输入暂未接入，默认认为药品已就绪 |

## 4. 当前引脚分配

| 引脚 | CubeMX 标签 | 外设/模式 | 代码宏 | 用途 |
|---|---|---|---|---|
| PH0 | OSC_IN | HSE 外部晶振输入 | 无 | 系统时钟 |
| PH1 | OSC_OUT | HSE 外部晶振输出 | 无 | 系统时钟 |
| PA13 | SWDIO | Serial Wire Debug | 无 | 下载/调试 |
| PA14 | SWCLK | Serial Wire Debug | 无 | 下载/调试 |
| PA6 | CAR_PWM_LEFT | TIM3_CH1 PWM | `CAR_PWM_LEFT_Pin` | 左电机 PWM |
| PA7 | CAR_PWM_RIGHT | TIM3_CH2 PWM | `CAR_PWM_RIGHT_Pin` | 右电机 PWM |
| PB0 | CAR_LEFT_AIN1 | GPIO_Output | `CAR_LEFT_AIN1_Pin` | 左电机方向 1 |
| PB1 | CAR_LEFT_AIN2 | GPIO_Output | `CAR_LEFT_AIN2_Pin` | 左电机方向 2 |
| PB2 | CAR_RIGHT_BIN1 | GPIO_Output | `CAR_RIGHT_BIN1_Pin` | 右电机方向 1 |
| PB10 | CAR_RIGHT_BIN2 | GPIO_Output | `CAR_RIGHT_BIN2_Pin` | 右电机方向 2 |
| PB5 | CAR_LED | GPIO_Output | `CAR_LED_Pin` | 状态 LED |
| PB6 | CAR_BEEP | GPIO_Output | `CAR_BEEP_Pin` | 蜂鸣器，也暂用于黄色 LED 兼容接口 |
| PB14 | USART1_TX | USART1 异步发送 | 无 | 调试串口 TX |
| PB15 | USART1_RX | USART1 异步接收 | 无 | 调试串口 RX |
| PC0 | GRAY_AD0 | GPIO_Output | `GRAY_AD0_Pin` | 八路灰度传感器通道选择 bit0 |
| PC1 | GRAY_AD1 | GPIO_Output | `GRAY_AD1_Pin` | 八路灰度传感器通道选择 bit1 |
| PC2_C | GRAY_AD2 | GPIO_Output | `GRAY_AD2_Pin` | 八路灰度传感器通道选择 bit2 |
| PC3_C | GRAY_OUT | GPIO_Input | `GRAY_OUT_Pin` | 八路灰度传感器公共输出 |

## 5. 灰度传感器引脚分配

当前 H743 工程已适配 `AD0/AD1/AD2/OUT` 型八路灰度传感器：

| 灰度模块信号 | 当前引脚 | CubeMX 标签 | 代码宏 | 方向 |
|---|---|---|---|---|
| AD0 | PC0 | `GRAY_AD0` | `GRAY_AD0_Pin` / `GRAY_AD0_GPIO_Port` | GPIO_Output |
| AD1 | PC1 | `GRAY_AD1` | `GRAY_AD1_Pin` / `GRAY_AD1_GPIO_Port` | GPIO_Output |
| AD2 | PC2_C | `GRAY_AD2` | `GRAY_AD2_Pin` / `GRAY_AD2_GPIO_Port` | GPIO_Output |
| OUT | PC3_C | `GRAY_OUT` | `GRAY_OUT_Pin` / `GRAY_OUT_GPIO_Port` | GPIO_Input |

平台层每次读取 `IOx` 时会扫描 8 个通道。当前读取极性与参考 MSP 代码一致：`OUT != 0` 视为检测到黑线。

## 6. 旧 CODE 传感器到当前灰度通道映射

| 旧 CODE 信号 | 当前映射 | 状态 |
|---|---|---|
| `IO1` | `gray_sensor_values[0]` | 已接入 |
| `IO2` | `gray_sensor_values[1]` | 已接入 |
| `IO3` | `gray_sensor_values[2]` | 已接入 |
| `IO4` | `gray_sensor_values[3]` | 已接入 |
| `IO5` | `gray_sensor_values[4]` | 已接入 |
| `IO6` | `gray_sensor_values[5]` | 已接入 |
| `IO7` | `gray_sensor_values[6]` | 已接入 |
| 第 8 路 | `gray_sensor_values[7]` | 已读取，旧逻辑暂未使用 |

## 7. 外设参数

| 外设 | 当前配置 | 用途 |
|---|---|---|
| TIM3 | Prescaler 5，Period 1999，CH1/CH2 PWM | 左右电机 PWM |
| TIM7 | Prescaler 239 | 保留为基础定时器 |
| USART1 | PB14/PB15，异步串口 | `u2_printf()` / `u3_printf()` 暂共用调试输出 |
| SysTick | HAL 默认 tick | `delay_ms()` 和主循环延时 |

## 8. 后续硬件接入建议

1. 编码器：不要复用 TIM3，因为 TIM3 已用于电机 PWM。建议在 CubeMX 中新增空闲定时器编码器模式，例如 TIM4/TIM5，确定引脚后再打开 `MED_CAR_ENABLE_ENCODER`。
2. 灰度循迹：当前只改底层读取，保留原先颜色判断逻辑；后续如果要充分利用第 8 路，可升级 `xunxian()` 的权重/位置判断。
3. 药品检测：确定输入引脚后打开 `MED_CAR_ENABLE_DRUG_SENSOR`，并将 `MedicineCar_ReadDrugPresent()` 改为读取真实 GPIO。
4. 蓝牙和识别串口：建议新增独立 UART，不与 USART1 调试串口混用。
5. TFT 和按键：建议先完成小车运动闭环，再逐项启用显示和人工控制模块。
